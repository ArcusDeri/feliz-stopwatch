# Feliz Stopwatch

This is an over-engineered solution for React stopwatch application built with Feliz and Elmish purely for educational purpose.

## To run the app
execute these commands in project root directory:
* `dotnet tool restore`
* `dotnet paket install`
* `npm i`
* `npm start`
